---
 - name: file creation 
   hosts: localhost
   tasks: 
       - name: at /usr
         file:
                  path: /usr/proxy.txt
                  state: touch
                  owner: Proxy_Server 

